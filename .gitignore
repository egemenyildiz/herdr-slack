# secrets — never commit
config.json
.env
.env.*
!.env.example

# runtime state
state/
*.log

# build + test output
dist/
coverage/
node_modules/
*.tsbuildinfo

# Internal design docs — kept locally, not shipped in the public repo
/docs/
